# @package _global_
trainer_params:
  gradient_clip_val: 0.5
  max_epochs: 400
  num_sanity_val_steps: 0
model_params:
  optimizer_params:
    lr: 0.0001
    weight_decay: 0.0
  ema_params:
    beta: 0.9999
    update_after_step: 128
    update_every: 1
  use_ema: true
